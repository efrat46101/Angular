import { CategoryPathPipe } from './CategoryPathPipe';

describe('PipePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryPathPipe();
    expect(pipe).toBeTruthy();
  });
});
